# Challenge 03: Palindrome Checker

**Difficulty:** 🟢 Beginner  
**Topics:** Strings, Functions, Logic

## 📖 Description

Create a function that checks if a given string is a palindrome (reads the same forwards and backwards).

## 🎯 Objectives

- Create a function called `isPalindrome` or `is_palindrome`
- Accept a string as input
- Return `true`/`false` (or `True`/`False` in Python)
- Handle both uppercase and lowercase letters

## 📋 Requirements

1. Function should be case-insensitive
2. Ignore spaces and special characters
3. Return boolean value (true or false)
4. Include at least 5 test cases

## 💡 Examples

```javascript
// Example in JavaScript
function isPalindrome(str) {
    // Your code here
}

// Test cases
console.log(isPalindrome("racecar"));      // true
console.log(isPalindrome("hello"));        // false
console.log(isPalindrome("A man a plan a canal Panama")); // true
console.log(isPalindrome("Madam"));        // true
console.log(isPalindrome("12321"));        // true
```

## 🌟 Bonus Points

- Make it work with numbers too
- Add a feature to find the longest palindrome in a sentence
- Optimize for performance

## 📚 Learning Outcomes

- String manipulation techniques
- Boolean logic
- Case handling and normalization
- Algorithm thinking
